A faire pour l'installation automatique :
-----------------------------------------


Verifier ce qui se passe avec make install pour les commandes MATLAB
Adapter une installation quelque part.

Produire les dépendances automatiques pour les fichiers compilés par mex
(une phase de compilation a vide) 

Produire le fichier de conf de mex suivant le compilo utilisé.

Configurer le make clean tel qu'il efface les cxx_repository (déjà ok ?)

shell en perl pour creer les fichier .am  (avec gestion automatique du numéro de version).

compilation conditionelle de matlabint et matlabcom et de misc

corriger le fait que des fonctions ont été ajoutées dans matlabint pour 
satisfaire le link avec cxx. Cela ne doit plus être nécessaire.
