<?xml version="1.0" encoding="UTF-8"?>
<refentry version="5.0-subset Scilab" xml:id="gf_mesh_im_get" xml:lang="en"
          xmlns="http://docbook.org/ns/docbook"
          xmlns:xlink="http://www.w3.org/1999/xlink"
          xmlns:xi="http://www.w3.org/2001/XInclude"
          xmlns:svg="http://www.w3.org/2000/svg"
          xmlns:mml="http://www.w3.org/1998/Math/MathML"
          xmlns:html="http://www.w3.org/1999/xhtml"
          xmlns:db="http://docbook.org/ns/docbook">
<refnamediv>
  <refname>gf_mesh_im_get</refname>
  <refpurpose></refpurpose>
</refnamediv>
<refsynopsisdiv>
  <title>Calling Sequence</title>
  <synopsis>
  </synopsis>
</refsynopsisdiv>
<refsection>
  <title>Parameters</title>

  <variablelist>
    <varlistentry>
      <term></term>
      <listitem>
        <para></para>
      </listitem>
    </varlistentry>
  </variablelist>
</refsection>
<refsection>
  <title>Description</title>
  <para>
  FUNCTION [x] = gf_mesh_im_get(meshim MIM, operation [, args])
  </para>
  <para>
  
  </para>
  <para>
    General function extracting information from mesh_im objects.
  </para>
  <para>
  
  </para>
  <para>
  * {I, CV2I} = gf_mesh_im_get(MIM,('integ'[, mat CVids])    Return a list of    integration methods used by the MeshIm.     `I` is an array of all Integ objects found in the convexes given in `CVids`.    If `CV2I` was supplied as an output argument, it contains, for each convex    listed in `CVids`, the index of its correspounding integration method in `I`.    Convexes which are not part of the mesh, or convexes which do not have any    integration method have their correspounding entry in `CV2I` set to -1.
  </para>
  <para>
    Example:
  </para>
  <para>
       cvid=gf_mesh_get(mim,'cvid');
  </para>
  <para>
       [f,c2f]=gf_mesh_im_get(mim, 'integ');
  </para>
  <para>
       for i=1:size(f), sf{i}=gf_integ_get('char',f(i)); end;
  </para>
  <para>
       for i=1:size(c2f),
  </para>
  <para>
         disp(sprintf('the integration of convex %d is %s',...
  </para>
  <para>
              cvid(i),sf{i}));
  </para>
  <para>
       end;
  </para>
  <para>
  * M = gf_mesh_im_get(MIM,('eltm',Eltm em, int cv [int f])    Return the    elementary matrix (or tensor) integrated on the convex `cv`.     **WARNING** Be sure that the fem used for the construction of `em` is    compatible with the fem assigned to element `cv` ! This is not checked by the    function ! If the argument `f` is given, then the elementary tensor is    integrated on the face `f` of `cv` instead of the whole convex.
  </para>
