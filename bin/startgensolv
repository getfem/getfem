#!/usr/bin/perl

print "loading and install for modifications the GenSolv library\n\n";

print "loading files from cvs\n";
`cd ..; cvs checkout getfem++/gensolv`; print "\n\n";

# print "Making links for C sources \n";
# `mkdir -p sources`;
# `cd sources; ln -sf ../gensolv/*.C .`; print "\n\n";

print "Making links for includes files\n";
`mkdir -p include`;
`cd include; ln -sf ../gensolv/*.h .`; print "\n\n";

# print "Making links for test progams\n";
# `mkdir -p programs`;
# `cd programs; ln -sf ../dal/test/* .`; print "\n\n";

print `bin/start_modif`;

